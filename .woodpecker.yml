version: "1"

steps:
  build:
    image: cirrusci/flutter:stable
    commands:
      - flutter pub get
      - flutter analyze
      - flutter test
      - flutter build apk --release
      - ls -la build/app/outputs/flutter-apk/
