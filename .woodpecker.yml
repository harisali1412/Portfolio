version: '1'

workspace:
  base: /flutter
  path: src

pipeline:
  setup:
    image: subfuzion/flutter:3.19.0
    commands:
      - flutter pub get

  analyze:
    image: subfuzion/flutter:3.19.0
    commands:
      - flutter analyze

  test:
    image: subfuzion/flutter:3.19.0
    commands:
      - flutter test

  build-web:
    image: subfuzion/flutter:3.19.0
    commands:
      - flutter build web --release
    when:
      event: push
      branch: main
